# Database Configuration
spring.datasource.url=jdbc:sqlserver://trank-ranker-server-123.database.windows.net:1433;database=track-ranker-db;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=trackrankeradmin
spring.datasource.password=Limenest46
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true

# Disable Flyway since tables already exist
spring.flyway.enabled=false

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE 

# SendGrid Configuration
sendgrid.api.key=${SENDGRID_API_KEY:}
sendgrid.from.email=prasun.dakotah@gmail.com
sendgrid.to.email=trackranker@proton.me